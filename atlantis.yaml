version: 3

#
# Projects
#
projects:

# Control Plane
- name: terraform-aws-baseline-iam
  dir: example
  autoplan:
    when_modified: ["example/**/*.tf", "example/basic"]
    enabled: true
  workflow: default-validator-plan
  terraform_version: v0.12.26
  apply_requirements: [mergeable, approved]
